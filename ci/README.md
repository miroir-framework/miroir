Act is setup, To test github actions locally:

```
  act -e act-env.json -W .github/workflows/build-linux-runnables.yml
```

```
  act -e act-env.json -W .github/workflows/build-sandbox-for-github-pages.yml
```