# 270 — Adversarial review of the TDD implementation plan

> Attack of `tdd-implementation-plan.md` against `analysis.md` (D1–D9; R1–R11 applied), `.agents/skills/miroir-analysis-to-tdd-plan/SKILL.md`, and the current codebase on branch `270-FEATURE-persistent-named-secrets`. Product decisions D1–D9 are not relitigated; slice verticality, RED runnability, GREEN feasibility/ordering, seam completeness, plan↔analysis consistency, and factual claims are. Every citation below was walked against the working tree on 2026-09-11.

**Applied:** 2026-09-11 — all P1–P18 ticked below and written into the revised `tdd-implementation-plan.md` (slices renumbered: MCP redaction is Slice 3; UI is Slice 7; nonreg is Slice 8).

**Verdict.** The plan is a faithful translation of the repaired analysis into a slice list: the tracer story is real (Slice 1 is the right first behavioral slice), the locked-defaults table matches the analysis decision record, every slice carries Status/RED/GREEN/Refactor/Validation with no commit lines, and the final slice is compliant (nonreg + docs + cleanup + AC). It is **not implementation-ready**. Two blockers: (1) Slice 0's characterization assertions are invalidated by Slice 1 GREEN, yet Slice 1's Validation re-runs them — the proof recipe cannot pass as written; (2) the Slice 1 tracer has no working persist→hydrate path in the emulated stack — no slice adds the `MiroirSecret` entity to the 5-of-9 `admin_model` test-asset copy the emulated-filesystem profile actually reads, and the write/read wiring (which DomainController, which store section, which read feeds `hydrateSecrets`) is never specified. Seven majors compound this: an internal contradiction in Slice 1's CLI parse (`import set is a separate field` vs `serverSecrets must still pass`), a missing authenticated-request seam for Slices 2/3/6 (no test today wires an identity directory into the emulated stack), an incomplete principal-thread inventory (the `handleActionInternal` composite hops are missing), an unspecified rotation-persist trigger that either breaks the existing hatch rotation test or makes the fail-closed RED untestable, a Slice 2 guard that silently breaks Slice 1's setup and the startup import path, a two-behavior Slice 6 that defers a **live** `passwordHash`-via-MCP leak to the last feature slice, and an MCP redaction test hedged into a package that cannot host it.

---

## Recommendations

- [x] **P1** [blocker] — Slice 0 characterization is re-run by Slice 1 Validation after Slice 1 GREEN has invalidated it: Slice 0 locks *current* behavior: "Admin Entity folder has **exactly 9** entities; no file named `MiroirSecret`; uuid `a96856df-…` is unused", "`--secrets-master-key` **throws** today" (true: `parseServerArgs.ts:110-112`), "`resolveSecret` is `(name: string) => string`" (true: `SecretStore.ts:14-23`), "`restServerDefaultHandlers` has no `/secrets` url" (true: `RestServer.ts:621-660`). Slice 1 GREEN adds the entity (9→10), adds the CLI flag, and changes the `resolveSecret` return type; Slice 2 adds the route. Yet Slice 1 Validation's first command is `RUN_TEST=secrets.270.phase0 npm run testByFile -w miroir-core -- secrets.270.phase0` — those assertions must fail after GREEN. The plan never says characterization tests are *consumed* (updated to assert the new contract) by the slice that flips them. → Fix: in each of Slices 1/2/3 GREEN, name the Slice 0 assertions being flipped and update `secrets.270.phase0.unit.test.ts` in place (e.g. "exactly 9" → "exactly 10 including `a96856df-…`"; "throws" → "parses `secretsMasterKey`"; signature assertion → new return shape). Keep the re-run in Validation only under that rule. Alternatively scope Slice 0 to contracts that survive (menu stays 8 items, `/queryTemplate` stays principal-less, MCP response branches unredacted until Slice 6) and drop the flipping assertions entirely.

- [x] **P2** [blocker] — Slice 1 Test B cannot persist or hydrate a `MiroirSecret` row in the emulated-filesystem stack; the plan dropped analysis R8-1 and never specifies the wiring: Three facts the plan does not mention: (a) the emulated-filesystem profile's Admin store reads `packages/miroir-standalone-app/tests/assets/admin_model` + `admin_data` (`miroirConfig.test-emulatedServer-filesystem.json`, deployment `18db21bf-…` = `deployment_Admin`), and that model copy holds only **5 of 9** entities (verified: AdminApplication, Deployment, bundle, ViewParams, ApplicationVersion — no `MiroirUser`/`MiroirUserCredential`/`MiroirRight`/`AiConfiguration`, and no `MiroirSecret`); a `createInstance` / `extractorInstancesByEntity` with `parentUuid: a96856df-…` fails entity resolution against that model. (b) Slice 1 GREEN adds the entity only to the real `miroir-test-app_deployment-admin` package. (c) The test never says *how* the row is written (server DC `createInstance`? direct store-section write? committed JSON fixture?) nor *what read* feeds `hydrateSecrets({ wrappingKey, rows })` — Test A passes `rows` in-memory, so Test B must read them back from the persistence store (analysis §5: boxed query, `queryExecutionStrategy: "storage"`, on the server DC — exposed as `domainControllerForServer` on the session result, `appStackIntegrationBootstrap.ts:319-326`) or the tracer proves nothing about persistence. → Fix: Slice 1 GREEN must (1) add the `MiroirSecret` entity JSON to `packages/miroir-standalone-app/tests/assets/admin_model/16dbfe28-…/` (and to the miroir-core / miroir-mcp copies if their suites touch the entity — see P9), or point `MIROIR_TEST_ADMIN_ASSETS_ROOT` (`IntegrationTestSession.ts:84-90`) at a tmp copy that includes it; (2) name the write path (server DC `createInstance` is fine *until Slice 2* — see P7); (3) name the hydrate read (server-DC storage-strategy boxed query on `MiroirSecret`, rows passed to `hydrateSecrets`); (4) state whether the session needs `openAdminAndMiroirStoresOnServer: true` (default is `false` in `AppStackIntegrationTestSession`, `IntegrationTestSession.ts:190-193`) or whether the deploy helper's admin-store open suffices.

- [x] **P3** [major] — Slice 1 Test A contradicts Slice 1's own refactor checkpoint on the CLI parse shape: Test A asserts "`--secret` no longer auto-fills a standing runtime map used as source of truth (**import set is a separate field**)", while checkpoint 1.3 requires "Existing `serverSecrets.unit.test.ts` must still pass (additive parse, not a break yet)" — and that test pins `parsed.secrets` toEqual `{ a: "b" }`, env fallback, and CLI-wins (`serverSecrets.unit.test.ts:33-79`). If "separate field" means renaming/splitting `parsed.secrets`, serverSecrets breaks in Slice 1, not Slice 5; if it means the same field with new treatment in `server.ts`, the parenthetical is unassertable and Test A over-specifies. → Fix: keep `ParsedServerArgs.secrets` unchanged (it *is* the import set; only `server.ts`'s use of it changes, in Slice 5), add only `secretsMasterKey?: string` in Slice 1, and delete the parenthetical from Test A. The parse-level break, if any, belongs to Slice 5 with the serverSecrets migration the plan already schedules in 5.3.

- [x] **P4** [major] — The authenticated-request seam for Slices 2/3 (and 6 Test B) does not exist in any current integ test and is never specified: Slice 2 RED says "`POST /secrets` … with Alice's Bearer"; Slice 3 RED needs Alice *and* Carol tokens, a `GET /secrets` listing filtered per principal, and "`POST /query` with Alice's Bearer". Today: `setupMiroirTest` creates the in-stack `RestClientStub` and wires the server DC + PSC manager into it (`setupMiroirTest.ts:65,123-124`) but **never returns it** and never calls `setIdentityDirectory` — the only `setIdentityDirectory` callers in the repo are production (`src/index.tsx:310`) and two miroir-core **unit** tests that build their own stub (`access.262.phase3.unit.test.ts:68-72`, `access.264.phase3.unit.test.ts`). The launcher forces `MIROIR_AUTH_ENABLED: "0"` (`testByFileLauncher.ts:45`) and the plan never says whether the secrets tests run hatch-on or hatch-off. The good news the plan should state: the stub extracts the principal from a Bearer token regardless of the hatch (`RestClientStub.ts:93-101`) and forwards it as `authPrincipal` (`:192`), so handler-level principal tests can run hatch-off with tokens minted via `setProcessTokenSecret(TEST_SECRET)` + `loginWithPassword` against a directory built from the real deployment-admin assets (the access.262 pattern, `:49-66`). → Fix: add a "Test fixture contract" note to Slice 2 (reused by 3/6): mint tokens per the access.262 pattern; either construct a test-local `RestClientStub` wired to the session's exposed `domainControllerForServer` + `persistenceStoreControllerManager` + identity directory, or extend `setupMiroirTest`/the session to return the in-stack stub; state the hatch posture per test; for the pure principal-thread assertions, allow calling `queryActionHandler` / server-DC `handleBoxedExtractorOrQueryAction` directly with `params.authPrincipal` (real handler, real DC — no mock).

- [x] **P5** [major] — Slice 3's principal-thread inventory is incomplete: it omits the `handleActionInternal` composite hops, so R5 silently re-opens for directly-dispatched composite queries: Slice 3 GREEN says "Thread `principal?: AuthPrincipal` through the inventory in analysis §5 (including composite)" and its Layers-cut line names `handleAction` → `handleApplicationAction` / `executeCompositeRunBoxedQueryAction` / `handleBoxedExtractorOrQueryAction` / `queryActionHandler`. But `handleAction` dispatches non-application actions to `handleActionInternal`, which reaches composite boxed queries at `DomainController.ts:3520-3521` (`compositeRunBoxedQueryAction` → `handleCompositeRunBoxedQueryAction`, private, `:4464-4486` → `executeCompositeRunBoxedQueryAction`, `:4489` → `handleBoxedExtractorOrQueryAction` with **two** args — no model environment, no principal — at `:4499-4513`) and `:3526-3533` (`compositeActionSequence` → `handleCompositeAction`, `:3567` → `handleCompositeActionTemplate`, `:4652`). Verified today: `handleAction` forwards `principal` to *neither* branch (`:2970-2976` and the `handleActionInternal` call both omit it; it only logs the principal, `:2918-2923`). An inventory that names `executeCompositeRunBoxedQueryAction` but not the two hops above it will thread the parameter into a function no caller reaches with a principal. → Fix: name the full hop list in Slice 3 GREEN: `handleAction` → (`handleApplicationAction` **and** `handleActionInternal`) → `handleCompositeAction` / `handleCompositeActionTemplate` / `handleCompositeRunBoxedQueryAction` / `handleCompositeRunBoxedQueryTemplateAction` → `executeCompositeRunBoxedQueryAction` → `handleBoxedExtractorOrQueryAction` (`:823`) / `executeBoxedExtractorOrQueryAction` (`:846`) / `resolveExtractorForExternalServiceInBoxedQuery` (`:3221`) → `executeExternalServiceOperation` (`:3037`, `:3260`). Keep the Slice 3 RED composite assertion (it is the right catch for exactly this).

- [x] **P6** [major] — Slice 4 never states when `persistRotatedSecret` fires, so it either breaks the existing hatch rotation test or makes its own fail-closed RED untestable: The existing `externalServiceQuery` suite registers secrets via the `registerSecrets` hatch with **no wrapping key** and asserts RAM rotation works ("rotated refresh_token from the token response is used on the next exchange", `externalServiceQuery.integ.test.ts:785-824`; rotation write at `ExternalServiceClient.ts:489`). Slice 4 RED asserts "If wrapping key is unset at persist time → fail closed; do not treat RAM as durable", and Slice 4 Validation re-runs `externalServiceQuery`. If the persist callback is wired unconditionally (or module-init), the hatch rotation test starts failing closed; if it is wired only where a wrapping key exists, then — per D3, rows + no key already fail at startup, and per D4 hatch secrets have **no backing row to update** — the "key unset at persist time" scenario is reachable only synthetically (key cleared mid-run), and the RED must say so. The plan also hedges the mechanism ("`setPersistRotatedSecret(callback)` **(or constructor-style register on SecretsService)**") and never says where the callback gets its persistence handle without `SecretsService` (4_services) importing `DomainController` (3_controllers) — the cycle the analysis R2 warned about (`DomainController.ts:165` already imports `ExternalServiceClient`). → Fix: state in Slice 4 GREEN: (1) the callback is optional and wired only at server startup / in the new tests — existing hatch tests never register it and keep RAM rotation; (2) persist fires only for **row-backed** secrets (the `resolveSecret` result must distinguish row-hydrated from hatch-registered — extend the Slice 1 return shape or track provenance in `SecretsService`); (3) the callback is a wiring-level closure over `serverDomainController` (actionLabel `secrets.set`), so `SecretsService` and `ExternalServiceClient` stay free of 3_controllers imports; (4) fail-closed = callback wired + row-backed secret + no held key. Drop the "or constructor-style" hedge.

- [x] **P7** [major] — Slice 2's guard silently breaks Slice 1's test setup and the startup import path, and Slice 2 Validation never re-runs the Slice 1 integ test: Slice 2 GREEN adds `assertSecretInstanceMutationAllowed` to `handleInstanceAction` (`DomainController.ts:1065`, sibling of the credential guard verified at `:1065` / `AuthenticationPolicy.ts:465-497`) and the CRUD REST handler (`RestServer.ts:276-287`), rejecting generic `createInstance`/`updateInstance` on `MiroirSecret` without `actionLabel: "secrets.set"`. Slice 1 Test B's setup writes a `MiroirSecret` row by an unspecified means (P2) — if that is a guardless `createInstance`, landing Slice 2 turns the Slice 1 tracer red, and nothing catches it: Slice 2 Validation runs only `secretsHttp.270` + `authentication.71` + `tsc`. The same hole applies to the **startup import upsert** (Slice 1 GREEN's "import+hydrate is allowed", Slice 5's flip): post-Slice-2, that upsert must carry `actionLabel: "secrets.set"` or it is rejected — the plan never says. → Fix: (1) Slice 1 names its write path (P2); (2) Slice 2 GREEN includes migrating Slice 1's setup to the guarded path (`POST /secrets` or `actionLabel: "secrets.set"`) and states the import upsert carries the label; (3) Slice 2 Validation adds `RUN_TEST=secretsHydrate.270 npm run testByFile -w miroir-standalone-app -- --profile emulatedServer-filesystem secretsHydrate.270`.

- [x] **P8** [major] — Slice 6 bundles two unrelated observable behaviors and defers a **live** security hole to the last feature slice: The skill's one-behavior-per-slice rule aside, the MCP half of Slice 6 is not a new-feature behavior: MCP tool **responses** serialize raw `returnedDomainElement` (`mcpHandlersForEndpoint.ts:286-295`) and raw `errorContext` (`:299-317`) **today** — an MCP `getInstances` on Admin returns `passwordHash` right now (Admin is always-allow, `AccessPolicy.ts:36-39`; only MCP *logs* are redacted, `:215`, `:250`, `:279`). The plan's own Slice 0 locks this as current behavior. The fix (wrap both response branches with the existing `redactCredentialSecretsFromValue`) needs no new entity for the `passwordHash` part and is independent of the `?page=secrets` UI story. → Fix: split Slice 6: land "MCP response redaction (passwordHash + ciphertext)" as its own slice immediately after Slice 1/2 (Slice 1 already extends the redactor with `ciphertext`; the response-branch wrap is one call site), and keep `?page=secrets` as the UI slice. At minimum, justify in the plan why a pre-existing credential leak waits for five slices.

- [x] **P9** [major] — Slice 6 Test A's location hedge is unresolvable as written, and its Validation command runs it in the wrong package: "**Test A:** `packages/miroir-mcp/tests/…` **or core unit that imports the formatter**" — the formatter (`handleMcpAction`) lives in `miroir-mcp`, which depends on `miroir-core`; a miroir-core unit test cannot import it (dependency direction + layering). Yet Slice 6 Validation's primary command is `RUN_TEST=secretsRedact.270 npm run testByFile -w miroir-core -- secretsRedact.270` with a trailing comment "# and/or miroir-mcp testByFile if the test lives there". → Fix: pin Test A to `packages/miroir-mcp/tests/unit/issues/270-persistent-named-secrets/secretsRedact.270.phase6.unit.test.ts` (miroir-mcp has `testByFile` = plain `vitest run`; existing precedent `mcpTools.integ.test.ts`), driving `handleMcpAction` with a fake payload — no store needed, so miroir-mcp's 5-entity admin copy does not block it. Make the miroir-mcp command the primary Validation line. If a redactor-only unit test is *also* wanted in miroir-core (the `ciphertext` strip lands there in Slice 1), it belongs in Slice 1's `secretsService.270.phase1.unit.test.ts`, not Slice 6.

- [x] **P10** [minor] — `handleSecretsHttpRoute` "sibling of `handleAuthHttpRoute`" is a layering trap if read literally: `handleAuthHttpRoute` lives in `1_core/authentication/AuthenticationHttp.ts` and uses only 1_core policy functions. The secrets handler must encrypt via `SecretsService` (planned for 4_services) — and 1_core cannot import a 4_services implementation (dependencies flow downward only). → Fix: name the file `packages/miroir-core/src/4_services/SecretsHttp.ts` (process-agnostic, same *pattern* as `AuthenticationHttp.ts`, taking `serverDomainController` as a `DomainControllerInterface` parameter — interfaces may flow both ways). Note the RestClientStub wiring point precisely: after the gate and the manager checks (`RestClientStub.ts:137-143`), before the `restServerDefaultHandlers.find` (`:165-170`), so `principal` (`:98-101`) and `this.serverDomainController` are both in scope.

- [x] **P11** [minor] — Slice 1 GREEN adds untested production behavior: "if wrapping key + import set are both present, import+hydrate is allowed" in `server.ts`: No RED covers this branch (miroir-server has no test suite — the plan's own header exception; the import unit tests are Slice 5). Untested GREEN in Slice 1 also pre-commits to an import path whose D6 alias assembler only exists from Slice 5. → Fix: restrict Slice 1's `server.ts` diff to `--secrets-master-key` parse + usage text + hydrate-only startup (rows exist + key → hydrate; rows + no key → fail closed is already D3 and testable via the orchestrator unit test). Move all import wiring to Slice 5, where its RED lives.

- [x] **P12** [minor] — The nonreg step `unit-270-persistent-secrets` contradicts manifest conventions: Slice 7.1 proposes one step mixing `testByFile -w miroir-core -- secrets.270` (unit) and standalone-app integ files with a hardcoded `--profile emulatedServer-filesystem`. In `scripts/nonreg-manifest.json`, integ steps use the `{profile}` placeholder at tier `default` (e.g. `appstack-PersistenceStoreController.integ`), unit steps are tier `unit`, and multi-command steps use `bash -c` (see `externalServices-spotify`). A hardcoded profile also defeats `npm run nonreg -- --profile …`. → Fix: two steps — `unit-270-persistent-secrets` (tier unit, core files) and `appstack-270-persistent-secrets` (tier default, `--profile {profile}`, the four standalone-app files) — or one `bash -c` step mirroring `externalServices-spotify`.

- [x] **P13** [minor] — Export-surface blast radius is unassigned: `packages/miroir-test-app_deployment-admin/index.d.ts` is a **committed** stub declaration file ("Auto-generated stub declarations for tsup consumers (dts: false)") listing every export — adding `entityMiroirSecret` to `index.ts` (Slice 1 GREEN) without the matching `index.d.ts` line breaks typed consumers. Likewise every new miroir-core export the tests import (`SecretsService` functions, `ENTITY_MIROIR_SECRET_UUID`, the new `resolveSecret` return type) must be added to miroir-core's 1200-line `index.ts` — no slice mentions either. → Fix: one line in Slice 1 GREEN ("export from admin `index.ts` + `index.d.ts`; export new symbols from miroir-core `index.ts`"). Docker, by contrast, *is* covered (Slice 7.2 names Dockerfile / docker-compose env docs per R7).

- [x] **P14** [minor] — Test-written secret rows land in a **committed** directory with no teardown story: the emulated-filesystem admin store's data dir is `packages/miroir-standalone-app/tests/assets/admin_data` (not tmp — the current git status already shows test-run dirt under `25d935e7-…/` and `7959d814-…/`). Slices 1–4 will leave `admin_data/a96856df-…/*.json` ciphertext rows behind on every run. Harmless (ciphertext) but noisy, and "No production `MiroirSecret` seed rows in git" (Allocated keys table) will be accidentally violated by a stray commit. → Fix: state the teardown (delete created rows in `afterEach`/`afterAll` via the same guarded path) or use the `MIROIR_TEST_ADMIN_ASSETS_ROOT` tmp-copy variant from P2; add the folder to the Slice 7 cleanup checklist.

- [x] **P15** [minor] — Slice 2 RED under-covers the guard: it asserts rejection of generic `createInstance` / `updateInstance` only, while D5 requires `deleteInstance` / `deleteInstanceWithCascade` rejection as well (the credential guard it mirrors covers all four, `AuthenticationPolicy.ts:465-497`). → Fix: add the two delete assertions to Slice 2 RED (GREEN's "CRUD REST handler" already implies them).

- [x] **P16** [minor] — Slice 6 Test B is unallocated and hedged ("standalone-app integ **or** component test"): the no-mocks rule makes a happy-dom component test with a stubbed fetch a mock by another name; the repo's UI integ precedent is the emulated stack (`externalServiceReport.integ.test.tsx`). Two facts that de-risk the page and should be written down: the auth gate comes **free** — `PageDispatcher` routes every `page !== "login"` through `nextPageWhenAuthGate` (`PageDispatcher.tsx:145-153`) — and only the query-param switch (`:168-204`) needs a `case "secrets"`; legacy path-segment mode (`:212+`) stays closed (same posture as #267 P17). → Fix: allocate `secretsPage.270.phase6.integ.test.tsx` (add the prefix to the Allocated keys table), vehicle = emulated-stack integ, and state "query-param mode only; no route constant exists — pages are switch cases".

- [x] **P17** [minor] — Slice 4 RED hedges the scope of the rotation proof: "the persisted process-scoped **(or user-scoped, separately)** row decrypts to `rotated-rt`". The user-scoped case is the entire point of D2-b + D7 (process fallback must update the **process** row, not a user row — analysis D7 step 3). → Fix: two explicit assertions — rotation of a process-backed token re-encrypts the process row; rotation of Alice's user-backed token re-encrypts Alice's row (and not the process row).

- [x] **P18** [minor] — The import **persist** leg is never integration-tested anywhere: Slice 5's unit test can cover import-set assembly, encryption, and post-import hydrate in memory (`importProcessSecrets` returns instances; persist is "the caller's job" per Slice 1 GREEN), but the upsert-into-Admin-store step (with `actionLabel: "secrets.set"`, post-P7) only ever runs inside `server.ts` wiring, which has no test suite. → Fix: add one integ assertion in the Slice 5 (or Slice 1) harness: run the orchestrator's import against the emulated admin store via `domainControllerForServer`, then `clearSecrets` + hydrate-from-store and resolve. Otherwise record "import upsert is wiring-only, untested" explicitly in Slice 5's Realization.

---

## Verticality (vector A) — per slice

| Slice | Observable behavior? | Layers cut? | Verdict |
|---|---|---|---|
| 0 | Characterization only (allowed) | Contracts / inventories | OK in principle; assertions must be consumed by the slices that flip them (P1) |
| 1 | Test operator stores ciphertext; Spotify query works without `registerSecrets` | Entity JSON → SecretsService → SecretStore → CLI parse → redaction → query path | Real tracer; persist/hydrate wiring unspecified (P2, P4, P7); parse contradiction (P3); untested server.ts import (P11) |
| 2 | Signed-in user sets/lists secret names; generic CRUD rejected | HTTP route → guard → encrypt+persist → stub+express wiring | Vertical; auth seam unspecified (P4); guard interaction with Slice 1 (P7); delete assertions missing (P15); file placement (P10) |
| 3 | Alice's token is Alice's; Carol gets no cross-served cache hit | User map → full principal thread → cache keys | Vertical; one behavior; inventory incomplete (P5); auth seam (P4) |
| 4 | Rotated refresh token survives restart | Callback → SecretsService → row update | Vertical; trigger/provenance unspecified (P6); scope hedge (P17) |
| 5 | Operator imports once, later launches need only the wrapping key; AI keys from the store | Import assembler → server wiring → `getApiKey` | Two behaviors but one launch-surface story — defensible grouping; persist leg untested (P18) |
| 6 | MCP responses redacted **and** `?page=secrets` form | MCP formatter → PageDispatcher | Two unrelated behaviors; live leak deferred (P8); test package wrong (P9); page test unallocated (P16) |
| 7 | Nonreg / docs / cleanup / AC | — | Compliant final slice; manifest step shape (P12); teardown (P14) |

## Feasibility notes that are *not* findings

These plan claims checked out against the working tree and should not be "fixed":

- `RestClientStub.call` really does dispatch `handleAuthHttpRoute` first (`RestClientStub.ts:74-91`), then gate + access checks, then `restServerDefaultHandlers.find` by method+url (`:165-170`), with `authPrincipal` on handler params (`:192`) and `serverDomainController` available (`:140-142`) — the Slice 2 wiring point exists. The change-password **Express** route persists via `domainController.handleAction(updateInstance)` with `AUTH_CHANGE_PASSWORD_ACTION_LABEL` (`server.ts:696-717`) while the stub path is in-memory-only — the plan's "do not copy the stub's in-memory persist" (2.3) is the right departure.
- `queryActionHandler` calls `handleBoxedExtractorOrQueryAction` with three args (no principal) at `RestServer.ts:503-507`, and already wraps the result in `redactCredentialSecretsFromValue` (`:508`) — so Slice 1's redactor extension automatically covers `/query` responses.
- The Slice 1 ↔ Slice 5 breaking-change sequencing is **consistent**: Slice 1 keeps `registerSecrets(parsed.secrets)` standing (`server.ts:169` untouched in behavior), and the R7 fail-closed flip is explicitly Slice 5's. No illegal early break of standing `--secret`.
- Test mechanics all resolve: miroir-core `testByFile` = `vitest run` (filename substring filter + `RUN_TEST` gating, e.g. `serverSecrets.unit.test.ts:20-24`); standalone-app `testByFile` supports `--profile emulatedServer-filesystem` (`testByFileLauncher.ts:30-57`); miroir-ai / miroir-mcp / deployment-admin all have `testByFile`; `RUN_TEST=authentication.71` runs all eight phase files (`startsWith` gating); miroir-server has no suite (header exception is factual).
- Inventories: 9 admin entities vs 5-entity partial copies in all three test-asset locations (re-listed); `a96856df-2b38-494a-8027-82617e2d64ad` is collision-free (repo-wide grep); admin `modelValidation.unit.test.ts` is filesystem-driven (`buildModelValidationGroupsFromFilesystem`) so the new entity is auto-included — free validation, correctly claimed.
- `MiroirTestExecutionEnvironment` exposes `domainControllerForServer` and `persistenceStoreControllerManager` (`appStackIntegrationBootstrap.ts:319-326`) — the persist-through-`serverDomainController` design (R3) is testable in-process without new framework seams.
- `getApiKey` is env-only with the "Missing environment variable" error (`copilotRuntimeFactory.ts:29-45`); the existing `copilotRuntimeFactory.unit.test.ts` and miroir-ai `testByFile` exist as Slice 5 assumes; miroir-ai → miroir-core import of `resolveSecret` is legal.
- The `?page=login` dispatcher pattern exists and the auth gate is generic over `page !== "login"` (`PageDispatcher.tsx:142-164`) — `?page=secrets` is gated for free.
- `docs/contributing/testing.md:36` does mandate the issue-directory cleanup (#238) that Slice 7.3 schedules; all four docs + Dockerfile + docker-compose targeted by Slice 7.2 exist.
- Slice 0's remaining factual assertions are accurate: 8-item admin menu, `parseServerArgs(["--secret","a=b"])` → `{ a: "b" }`, unknown-option throw, name-keyed `oauth2AuthorizationCodeCacheKey` (`ExternalServiceClient.ts:386`), no-principal `handleApplicationAction` signature (`DomainController.ts:2989`), unredacted MCP success branch (`mcpHandlersForEndpoint.ts:286-295`), no `/secrets` in `restServerDefaultHandlers` (`RestServer.ts:621-660`).

## Coverage vs R1–R11 (vector G)

| Repair | Slice | Status |
|---|---|---|
| R1 principal-scoped caches | 3 | Placed; cache-key helper checkpoint present |
| R2 `persistRotatedSecret` + held wrapping key | 4 | Placed; trigger/provenance/wiring under-specified (P6) |
| R3 `handleSecretsHttpRoute` in server.ts **and** RestClientStub, persist via server DC | 2 | Placed and factually feasible; placement + auth seam gaps (P4, P10) |
| R4 MCP **response** redaction | 6 | Placed but deferred and bundled (P8); test package wrong (P9) |
| R5 principal through composite boxed-query path | 3 | Claimed; inventory missing hops (P5) |
| R6 SecretsService in miroir-core, server.ts wiring-only | 1, 5 | Placed; Slice 1 wiring exceeds its tests (P11) |
| R7 import-without-key fails | 5 | Placed; consistent with Slice 1 non-break |
| R8 blast radius | 1, 7 | **Partially dropped**: modelValidation auto-include claimed; test-asset entity copies + `index.d.ts` unassigned (P2, P13); `LIVE_SPOTIFY_*` hatch kept (Slice 5 GREEN ✓) |
| R9 only `POST /query` needs principal | 3 | Placed ("/queryTemplate unchanged") |
| R11 transactional/commit-replay guard bypass as debt | 2 | Placed (one-line comment in GREEN) |

## What the plan got right

- **The tracer is the right tracer.** Slice 1's story — ciphertext row on Admin `MiroirSecret`, decrypted with `test-secrets-master`, `extractorForExternalService` succeeds against the fake Spotify server *without* `registerSecrets` — is exactly the thinnest end-to-end proof of the feature, with an independent fail-closed control. Its failure is wiring specificity (P2), not conception.
- **Locked defaults are the analysis decision record**, not a rewrite: D1–D9 match, R7's break is correctly sequenced to Slice 5 with Slice 1 explicitly non-breaking, and the non-goals (PKCE UI, capability gate, #263, Deployment passwords, keychain, ExternalService split, server-only flag) carry their owning issues.
- **The R3 departure is correctly understood**: the plan knows the change-password stub path is in-memory-only and explicitly refuses to copy it (2.3), persisting through `serverDomainController` with a dedicated `actionLabel` instead.
- **Real mechanisms, no mocks**: every RED targets the real DomainController / emulated server / fake HTTP server; the vitest justifications (crypto, CLI, startup orchestration, HTTP route policy, UI) are the legitimate exceptions, consistent with the #267 precedent.
- **Template compliance**: Status lines, named RED files + behavior assertions, GREEN notes, refactor checkpoints, Validation commands, Realization placeholders, progress table, allocated uuids/keys, no commit lines, human-in-the-loop resume note, AC checklist mapping each issue criterion to its proving slice, and the #238 issue-directory cleanup in Slice 7.
- **Slice 3's RED assertions are the right ones** for R1 (two principals, warm cache, no cross-serving) and R5 (composite boxed query receives the outer principal) — they will catch exactly the defects the analysis review found, once the inventory (P5) and the auth seam (P4) are specified.

## Suggested repair order (not new product decisions)

1. Fix P2 + P4 in Slice 1/2 (test-asset entity copy or tmp assets root; write/read wiring; token-minting + stub seam) — everything else stands on the tracer running.
2. Resolve the Slice 0/1 contradictions (P1, P3) and narrow Slice 1's server.ts diff (P11).
3. Specify Slice 2's guard interactions (P7) and route placement (P10); complete the Slice 3 inventory (P5).
4. Pin Slice 4's persist trigger + provenance (P6, P17).
5. Split Slice 6 (P8), pin the MCP test to miroir-mcp (P9), allocate the page test (P16).
6. Sweep the minors into Slice 7 (P12–P14) and the RED additions (P15, P18).
