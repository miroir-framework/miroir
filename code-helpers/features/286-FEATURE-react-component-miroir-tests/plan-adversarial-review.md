# 286 adversarial review of the TDD implementation plan

> Attack of `tdd-implementation-plan.md` against `analysis.md` (post R1-R20, decisions D1-D13 not relitigated), `docs/contributing/testing.md`, issue #286 acceptance criteria (`gh issue view 286`), and the code on `286-FEATURE-react-component-miroir-tests` at `b04820a3e`: `JzodElementEditorTestTools.tsx`, `JzodElementEditor.test.tsx`, the 14 importers, `MiroirTestTools.ts`, `miroirTestSuiteWalk.ts`, `test-expect.ts`, `RunMiroirTestSuiteButton.tsx`, `RunAllMiroirTestsButton.tsx`, `MiroirTestListDisplay.tsx`, both `*IntegrationLaunch` harnesses and their mocks, `tests/setup.ts`, both `vite.config.js`, `test-by-file.ts`, `testByFileLauncher.ts`, `scripts/nonreg-manifest.json`, `scripts/run-nonreg.py`, `miroir-server` scripts and `server.ts`, and the current `miroir-standalone-app/dist`.

**Verdict.** The slice order is sound: characterization, a refactor under green, a one-case tracer through the real walk, then the pilot with a user stop point before Run all and the five remaining suites. The case counts are right (68, then 67, 56, 53, 50, 36, 24, 15, 0 left in the old file). The plan is not ready to start. Slice 1 cannot stay green as written, because `getWrapperLoadingLocalCache` owns module state that the #274 and #284 tests read, and the wait helpers it moves depend on RTL `act`. Two of the 14 importers already fail today, and `--bail=1` hides the rest. Several assertions hardcode counts that later slices make false. Four tests cannot be written as described: the jest-dom parity test in miroir-core (no DOM, no jest-dom), the sandbox test on a harness that mocks the results panel away and needs Postgres, the bundle check (the Vite manifest has no module list, and the check is green before any change), and the `IS_REACT_ACT_ENVIRONMENT = false` line that RTL resets in `beforeAll`. The rebase target is wrong: PR #285 merged into `aba`, not `master`. Fix P1 to P13 before Slice 1.

**Dispositions (all 22 applied in tdd-implementation-plan.md):** P1 → Slice 0.1 per-importer baselines, the two red importers fixed in Slice 0, one command per file. P2 → Deviations, Slice 1.1 `buildComponentTestWrapper` return value, RTL wait helpers stay in the tests file. P3 → phase4 `testFilter` on the Array sub-suite, phase6 count from the manifest, re-run only after GREEN. P4 → phase0 reads the old file as text, expected suite list updated per slice. P5 → parity test moved to the app issue directory, miroir-core test on fakes, message argument case. P6 → `beforeAll` flag with an entry check, dom config saved and restored in UI tests. P7 → Slice 4.3 guard over sourcemaps, `@testing-library/react` and `TransformerBuilderPage` check on the component chunk. P8 → guard fails on a missing or stale build, kept out of nonreg. P9 → Slice 4.1 harness over a real `LocalCache`, results from the tracker, Postgres noted for the launch tests. P10 → `componentTestMode.286.phase4`. P11 → Slice 2.1 stubs first, failing-runner checks moved to the miroir-core file with runner restore. P12 → Slice 0.0 rebase onto `origin/aba` after user confirmation. P13 → Slice 4.4 production commands and log line. P14 → deployment rebuild and modelValidation after each generator run. P15 → generator writes `index.d.ts`. P16 → tsc baseline and no-new-error rule. P17 → saved case list and diffs in 5.3 and Slices 7-11. P18 → consistency fixtures and idempotence in Slice 3 RED. P19 → `.tsx` phase0, no `RUN_TEST`. P20 → `docs/contributing/testing.md` in 12.1. P21 → checkbox on the unit button only, `vi.mock` call count, small suite. P22 → `destroy` and wrapper spies in phase4.

---

## Citation and command audit

| Claim in the plan | Result |
|---|---|
| Old suite has 7 suites, 68 cases, per-suite counts 12/3/3/14/12/9/15 | Consistent with `jzodElementEditorTests` (test L3485-3558) and analysis §3.1. Running totals and "old suite left" are correct. |
| 14 importers of `JzodElementEditorTestTools` | Verified: 14 files besides the tools and `JzodElementEditor.test.tsx`, including the two rigs `gridPaginationIntegRig.tsx` and `listTransformerIntegRig.tsx`. The three Slice 1 `testByFile` lines reach all of them. |
| `testByFile` accepts several filters and `-t` | Verified. `stripProfileArgs` only removes `--profile`/`--storage` pairs, the rest goes to `vitest run`. But the launcher adds `--bail=1` (`test-by-file.ts` L27), so a multi-filter line stops at the first failing file. |
| `RUN_TEST=<name>` | Works only if the test file implements the guard itself (e.g. `externalService.unit.test.ts` L24-28). vitest ignores the variable. |
| miroir-core `testByFile`, `testMiroir` | Both exist (`miroir-core/package.json` L42-43). miroir-core vitest runs in the node environment: no `environment`, no `setupFiles` (`miroir-core/vite.config.js` test block). |
| `tests/1_core/issues` in miroir-core | Exists (`273-process-capability-switches`, `275-cursor-sdk-copilotkit-backend`). |
| `npm run nonreg -- --tier default` | Valid (`run-nonreg.py` L412-417, `package.json` L26). |
| `modelValidation.unit.test.ts` filter | Correct for `test.root: "./tests"` (`deployment-miroir/vite.config.js` L16). |
| Schema rebuild order | Correct: `generate-ts-types.ts` L394 reads `entityMiroirTest` from the deployment package dist. |
| `build.manifest: true` "if it is not set" | Not set. The current `dist/` has no `.vite/` folder. |
| `miroir-server/release/client` | The folder exists, filled by `miroir-server/scripts/copy-client.mjs`. The plan gives no command to build, copy, or serve it. |
| Baseline `extractValuesFromRenderedElements.test.tsx` | Fails today: `TypeError: Cannot read properties of undefined (reading 'querySelectorAll')`, 1 failed of 4, then bail. |
| Baseline `formValuesToJSON.test.ts` | Fails today: 1 failed, 2 passed of 6, then bail. |
| Baseline `tsc` | miroir-core: 0 errors. miroir-standalone-app: 1 error (`JzodElementEditorHooks.ts(528,59)` TS2339), and its `include` covers `src/` only. |
| PR #285 state | `MERGED` into `aba` (`gh pr view 285`). `origin/master` is `d1fbd1c98` and does not contain `572f5b27f`. |

---

## Recommendations

- [x] **P1** [blocker] — Two of the 14 importers are red before any change, and `--bail=1` hides the rest. `tests/extractValuesFromRenderedElements.test.tsx` calls `extractValuesFromRenderedElements(expect)` with no container, and the tools read `(container as any).querySelectorAll(...)` unconditionally (`JzodElementEditorTestTools.tsx` L1690-1691). A run on this branch gives a TypeError, 1 failed of 4. `formValuesToJSON` gives 1 failed, 2 passed of 6. Slice 1 Validation L176 puts 5 filters on one line, so the first red file stops the run. "Each of the 14 importers gives the same count as before the slice" (L183) has no recorded "before". → Add to Slice 0 a baseline table: one `testByFile` command per importer file, with its pass/fail/skip counts. Name the two known-red files as pre-existing, and either fix them in Slice 0 (pass `container`) or exclude them from the "same count" rule. Split the Slice 1 and Slice 3 multi-filter lines (L176, L306) into one command per file.

- [x] **P2** [blocker] — Slice 1 is not a safe move as described. (a) `getWrapperLoadingLocalCache` writes module state `jzodEditorTestLocalCache` and `jzodEditorTestApplicationDeploymentMap` when `wireLocalCacheCompositeAction` is set (tools L976-979). `getJzodEditorTestLocalCache` and the upsert/delete/restore helpers read it (L266-400). `multistepProcess.274`, `multistepLaunch.274`, `multistepBranch.284`, and `wizardWalk.284` import those helpers. The function returns only a `React.FC`, so a tests-side wrapper that "delegates" to a `src/` copy cannot see the cache. Slice 4 needs the same handle for `miroirEventService.destroy()`. (b) `waitForProgressiveRendering` and `waitAfterUserInteraction` (L200-229) use RTL `waitFor`, `act`, `screen`, and `process.env`. Checkpoint 1.2 forbids `@testing-library/react` in `componentTestTools.tsx`, so moving "the wait helpers" changes them for 9 importers (both `gridPagination` tests, both `listDisplayByTransformer` integ tests, `virtualAttributes`, both #274 tests, both #284 tests). → Give the `src/` function a return value like `{ Wrapper, localCache, miroirEventService, applicationDeploymentMap }`, and have the tests-side `getWrapperLoadingLocalCache` set the module state from it and return `Wrapper`. Say where the `wireLocalCacheCompositeAction` branch (real `DomainController`, `MemoryRouter`, `persistenceSaga.run`) lives. Keep the RTL wait helpers in the tests file. Add act-free equivalents to `componentTestEnvironment.ts` in Slice 2.

- [x] **P3** [major] — Hardcoded counts go false in later slices. `componentTestSandbox.286.phase4` asserts 12 `ok`, and 11 `ok` plus 1 `error` (L332-333), on a unit Run of the whole instance. Slice 5 adds 3 Enum leaves and re-runs phase4 in its Validation (L409), which then sees 15 leaves. `runAllComponentTests.286.phase6` asserts 15 `ok` (L434), but Slices 7-11 add leaves, and their RED stubs throw "not migrated". → Run phase4 with a `testFilter` restricted to the `JzodArrayEditor` sub-suite (the `testFilter` prop exists on `RunMiroirTestSuiteButton`, L48). Derive the phase6 expected count from `componentTestManifest.ts` at test time, and state that phase6 is only re-run after a GREEN.

- [x] **P4** [major] — The inventory assertion "`JzodElementEditor.test.tsx` registers 7 suites" (L131) flips at Slice 3, not Slice 12. Slice 3 GREEN deletes `JzodArrayEditor` from `jzodElementEditorTests` (L292). Slices 5 and 7-11 remove more. The phase0 test also cannot import the old file to count its suites, because the import registers its `describe` and its 67 cases in the phase0 run. → Read the old file as text and assert the suite key list. Update the expected list in each slice that deletes a suite, or replace the assertion with "the old file exists" and delete it in Slice 12. Add the phase0 run to the Validation of Slices 3, 5, and 7-11.

- [x] **P5** [major] — `throwingExpect.286.phase3` cannot run "through jest-dom" in miroir-core. The miroir-core vitest config has no `environment` and no setup file, and `miroir-core/package.json` declares neither `happy-dom` nor `@testing-library/jest-dom` (both resolve only through root hoisting). The same RED also misses a contract the bodies use: vitest's second `expect` argument is a message (`expect(beforeTestResult, "before up button click")`, test L226 and L251, and `expect(false, "number textBox content is not a number for " + name)` in the tools, L1628, L1682, L1798), while miroir-core `expect(actual, testName)` treats it as a test name. → Move the jest-dom parity cases to `packages/miroir-standalone-app/tests/4_view/issues/286-react-component-miroir-tests/`, which has happy-dom and jest-dom through `setup.ts`. Keep the miroir-core test for non-DOM semantics (`toEqual` with `undefined` keys, `getState`, the thrown message) plus DOM matchers on minimal element fakes, with no `instanceof HTMLInputElement`. Add a case where a second argument appears in the thrown message.

- [x] **P6** [major] — `IS_REACT_ACT_ENVIRONMENT = false` at file scope (analysis §5.8 step 1) does not hold during the tests. `setup.ts` imports `@testing-library/react` with `globals: true`. RTL 16.3 registers a `beforeAll` that calls `setReactActEnvironment(true)` (`@testing-library/react/dist/index.js` L41-46), and that hook runs after the entry's module scope. The vitest run then differs from the browser. The `setup.ts` filter on `console.error` also hides act warnings that could reveal K1. The same issue hits phase4 and phase6: they mount `MiroirTestDisplay` with RTL `render`, and `componentTestEnvironment`'s `configure({ eventWrapper: cb => cb(), asyncWrapper: cb => cb() })` then removes the `act` wrapping from the test's own `fireEvent` and `waitFor`. → Set the flag in a `beforeAll` inside the entry and assert it in the tracer body. In phase4 and phase6, save the `@testing-library/dom` config before the run and restore it after, or drive the Run button with `@testing-library/dom` only.

- [x] **P7** [major] — `componentTestChunk.286.phase4` is not red, and its mechanism is not stated. The current build already puts `@testing-library/react` and `@testing-library/dom` in a lazy chunk: `dist/assets/standaloneAppBrowserIntegrationOrchestrator-*.js.map` lists both, through `src/miroir-fwk/4-tests/tests-utils.tsx` L1-2. That chunk is reached by `await import(...)` from `loadBrowserUiIntegrationTestLauncherEnvironment-*.js`. "No statically imported chunk contains `@testing-library`" therefore passes before any #286 code. A Vite manifest lists chunk files, `imports`, and `dynamicImports`, not the modules inside a chunk. The module list is only in the `.map` files (`build.sourcemap: true`, `vite.config.js` L45). D2 ("no `@testing-library/react` in the chunk") has no check. → Call it a guard, not a RED. Specify the algorithm: take the static `imports` closure from the `index.html` entry in the manifest, and read each chunk's `.map` `sources` for `node_modules/@testing-library/`. Add: the chunk closure reached by `ComponentTestSandboxProvider`'s `import()` contains no `@testing-library/react` and no `TransformerBuilderPage`. Record in the #197 doc update (Slice 12) that the integration launcher chunk already loads RTL.

- [x] **P8** [major] — Slice 12 adds `componentTestChunk.286.phase4` to the unit nonreg step (L512), but `dist/.vite/manifest.json` exists only after `npm run build -w miroir-standalone-app` (`node --max-old-space-size=4096 ... vite build`, `package.json` L13). No nonreg step builds the app, and a stale `dist/` gives a stale answer. → Make the test fail with a clear "run the build first" message when the manifest is missing or older than `src/`. Put it in its own step that runs the build first (default or full tier), or keep it out of nonreg and in the Slice 4 and Slice 12 Validation only.

- [x] **P9** [major] — The Slice 4 and Slice 6 tests cannot be written as described. "Mount as `MiroirTestDisplayIntegrationLaunch.integ.test.tsx` does" (L330) implies `miroirTestDisplayIntegrationLaunchMocks.ts`, which mocks `TestExecutionPanel` to `null` (mocks L12-14), and whose launcher uses `emulatedServer-sql` (nonreg `requires: "postgres"`, L781). That harness has no app `LocalCache` (`domainController={{} as DomainControllerInterface}`), so "app `LocalCache` state equals the snapshot" has nothing to compare. No existing test renders `TestResultsGrid` in happy-dom, and it renders through `ValueObjectGrid`. The plan header also says "no mocks beyond `handleAction`", which that harness contradicts. `MiroirTestListIntegrationLaunch` has the same problems (`requires: "postgres"`, L798). → Specify the harness: `MiroirContextReactProvider` plus `LocalCacheProvider` over a real `LocalCache` seeded with the meta-model, no launch mocks, no Postgres. Read results from `miroirActivityTracker.getTestAssertionsResults([])` and from `onTestComplete`/`testResultsData`, not grid cells. State that the `MiroirTestDisplayIntegrationLaunch` and `MiroirTestListIntegrationLaunch` runs in the Slice 4 and Slice 6 Validation (L362, L451) need Postgres, or pass `--profile` as nonreg does.

- [x] **P10** [major] — `ComponentTestModeContext` (analysis §5.4, G7) has no test. It appears only in Slice 4 GREEN (L345). vitest cannot see its effect by default: `test.env.VITE_TEST_MODE` is `'true'` (`vite.config.js` test block), and `JzodElementEditor` computes `isUnderTest` at module load (`JzodElementEditor.tsx` L92-95). Only the manual check covers it. → Add a phase4 unit test: `vi.stubEnv("VITE_TEST_MODE", "false")`, `vi.resetModules()`, a dynamic import of `JzodElementEditor`, then render an object schema with the context `{ progressiveRenderDisabled: true, codeMirrorPlaceholder: true }` and assert the `<pre>codeMirrorValue:` box and no `Loading ...` placeholder. Without the context, assert the opposite.

- [x] **P11** [major] — The Slice 2 RED is red for the wrong reason. Per analysis §5.8 step 1, the entry imports the registry directly, and also the runner. Before GREEN those modules do not exist, so vitest fails with "Failed to resolve import", not with "instance missing" or "unknown leaf kind" (L213). The second test, "a runner whose case body throws" (L211), fails its own vitest test under `rethrowComponentTestFailures: true`. It also replaces the global runner in `ConfigurationService` while the first test's leaves are still queued, because the walk registers leaves with `localVitest.test` and runs them later (`miroirTestSuiteWalk.ts` L206-235). → Start Slice 2 RED with empty stubs of `componentTestManifest.ts`, `componentTestRegistry.ts`, and `runReactComponentTest.tsx`, so the RED fails on "suite `JzodElementEditor_ComponentTestSuite` not found" and then on `Unknown miroirTestType: reactComponentTest`. Run the failing-runner check in-process with `TestFramework` and rethrow unset, and restore the runner in `finally`. Count that test in the totals (1 becomes 2, 12 becomes 13, and so on), or move it to the miroir-core phase2 file.

- [x] **P12** [major] — The rebase note (L8) targets the wrong branch. `gh pr view 285` shows `state: MERGED`, `baseRefName: aba`. `git branch -r --contains 572f5b27f` lists only `origin/284-FEATURE-openapi-connection-wizard` and `origin/aba`. `origin/master` (`d1fbd1c98`) has no #284 code. A rebase onto `master` drops `multistepBranch.284` and `wizardWalk.284`, which Slice 1 Validation (L177) runs. → Rebase onto `origin/aba` now, or wait for `aba` to reach `master`. Rebase before Slice 1 so the importer baseline (P1) is taken on the final base.

- [x] **P13** [major] — The production-build check (4.3, L353) has no procedure. The actual path is: `npm run build -w miroir-standalone-app`, then `npm run copy:client -w miroir-server` (`copy-client.mjs` copies `miroir-standalone-app/dist` to `release/client`), then `npm run build:release -w miroir-server`, then `npm run run:prod -w miroir-server`. The server serves the SPA only when `getMiroirEnvironmentMode() === 'prod'` (`server.ts` L878-888). The #202 plan records about 10 minutes for the full artefact build. → Write these commands in 4.3 and 5.3, with the expected mode log line ("Running in production mode - serving React SPA"). Say that `.vite/manifest.json` gets copied into `release/client` and is then served publicly. Unverified alternative: `vite preview`, which reuses `server.proxy` by default.

- [x] **P14** [minor] — Slices 3, 5, and 7-11 re-run the generator but not `npm run build -w miroir-test-app_deployment-miroir`. tsup bundles the JSON into `dist/index.js` (`tsup.config.js` json loader, `package.json` `"main": "dist/index.js"`), so the dev app, `modelValidation`, and any test importing the named export see the old leaves. → Add the deployment rebuild and `modelValidation.unit.test.ts` after each generator run.

- [x] **P15** [minor] — `miroir-test-app_deployment-miroir/index.d.ts` is a hand-kept stub (`"types": "index.d.ts"`, 62 `miroirTest_` declarations, no script writes it). The generator adds the export to `index.ts` and `Model.ts` only (analysis §5.7). → Have the generator also add `export declare const miroirTest_<name>: any;` to `index.d.ts`, and check it in the consistency test.

- [x] **P16** [minor] — The `tsc` checks do not mean "no new error". miroir-standalone-app has 1 error today (`JzodElementEditorHooks.ts(528,59)` TS2339), and its `tsconfig.json` includes `src/` only (L16-22), so the tests-side re-export and the 14 importers are never type-checked. → Record the baseline error count in Slice 0 and assert "no new error". Do not cite `tsc` as evidence for the importers.

- [x] **P17** [minor] — The pilot checkpoint compares "case by case" with the Slice 0 baseline (5.3, L399), but Slice 0 records only "68 passed" (L144). → Save the verbose case list (name and status) of the old run in the Slice 0 Realization, and diff it against the new entry's verbose list in 5.3.

- [x] **P18** [minor] — The consistency test and the generator have no RED. `componentMiroirTests.consistency.unit.test.ts` appears only in Slice 3 GREEN (L291), so issue AC "the generator script and the consistency check keep the JSON leaves and the TypeScript registry in step" has no failing proof. → In Slice 3 RED, add fixtures: a manifest case with no JSON leaf fails, a registry case missing from the manifest fails, a duplicate leaf label fails. Add a generator idempotence check (a second run leaves `git diff` empty).

- [x] **P19** [minor] — Test file details. `componentMiroirTests.286.phase0.unit.test.ts` renders a form for the `extractValuesFromRenderedElements` fixture, so it needs `.tsx` or `React.createElement`. The plan uses `RUN_TEST=<name>` for every issue test, but the variable does nothing unless the file implements the guard. → Rename to `.tsx`. State that each new issue test copies the `RUN_TEST` guard from `externalService.unit.test.ts` L24-28, or drop the prefix from the commands.

- [x] **P20** [minor] — Slice 12 docs miss `docs/contributing/testing.md` L176-192 ("JzodElementEditor component tests"), which gives the old `JzodElementEditor.test` commands and "(67 tests total)". → Add it to 12.1 with the new entry, `-t "<suite>"`, and the generator.

- [x] **P21** [minor] — Slice 6 details. `MiroirTestListDisplay` renders `RunAllMiroirTestsButton` twice, in unit mode (L170) and in integration mode (L183). "The component test chunk is not loaded" has no stated observable in vitest. `miroirCoreTransformers`, the suite the list harness uses, is large for a unit Run all. → Put the checkbox on the unit instance only. Observe the chunk load through a `vi.mock` of `componentTests/index.ts` that counts calls to `registerComponentTests`. Pick a small transformer suite.

- [x] **P22** [minor] — `miroirEventService.destroy()` on the last case and on close (analysis §5.6 step 5), and one wrapper per suite (step 2), have no assertion. → In phase4, spy on `MiroirEventService.prototype.destroy`: one call after a run of one suite, none left after close, and one `getWrapperLoadingLocalCache` call per suite.

### Coverage of the issue acceptance criteria

| Acceptance criterion | Slice and test | Gap |
|---|---|---|
| Schema leaf and runner dispatch | 2, `reactComponentLeaf.286.phase2` and the entry | P11 |
| Generic entry reports skipped | 2, phase2 and `testMiroir` | none |
| 68 cases from JSON, same results | 2-11, new entry | P17 for the comparison |
| Unit Run button, sandbox, results in `TestResultsGrid` | 4, phase4 and manual | P9, P10 |
| Failing DOM assertion recorded, later cases run | 4, phase4 | use a failing DOM matcher, not a generic throw |
| App store and local cache unchanged | 4, phase4 | P9 |
| Main bundle has no `@testing-library/*` | 4, chunk check | P7, P8 |
| Generator and consistency check | 3, consistency test | P14, P15, P18 |
| Run all checkbox | 6, phase6 | P3, P21 |
| Old file deleted, nonreg runs the new entry | 12 | P8 |
| #197, #204, testing docs point to #286 | 12 | P20 |

Analysis §5 mechanisms with no test in any slice: `ComponentTestModeContext` (P10), `destroy()` and one wrapper per suite (P22), and the absence of React `act` and `@testing-library/react` in the chunk (P7). The last one is only checked by hand in the production build.
