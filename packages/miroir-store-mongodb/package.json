{
  "name": "miroir-store-mongodb",
  "private": true,
  "version": "0.0.0",
  "description": "The Miroir implementation for accessing MongoDB databases.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "type": "module",
  "dependencies": {
    "miroir-core": "*",
    "mongodb": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "copyfiles": "^2.4.1",
    "rimraf": "^4.1.1",
    "tsconfig-paths": "^4.1.2",
    "tsup": "^8.0.1",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "watch": "lerna watch --scope='miroir-store-mongodb' --include-dependencies -- lerna run build --scope='miroir-store-mongodb'",
    "build": "tsup",
    "build4": "npm run build",
    "build5": "npm run build",
    "copy-files": "copyfiles -u 1 src/**/*.json src/**/*.css dist/src",
    "test": "vitest"
  },
  "author": "Miroir Framework <miroir-framework@protonmail.com>",
  "license": "MIT"
}
