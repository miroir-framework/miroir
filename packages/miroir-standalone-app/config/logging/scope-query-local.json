{
  "$comment": "Scoped: query path, narrow (Path A local-cache hop only). LocalCache + query selectors at DEBUG; DomainController stays at WARN so it does NOT re-surface the (demoted-to-DEBUG) composite/action dumps. Use as the second pass after a `catch-all` orientation run when you only need query payload detail on the leaf.",
  "defaultLevel": "WARN",
  "defaultTemplate": "[{{time}}] {{level}} {{name}} ### ",
  "specificLoggerOptions": {
    "4_miroir-localcache-redux_LocalCache": { "level": "DEBUG" },
    "2_miroir-core_QuerySelectors": { "level": "DEBUG" },
    "2_miroir-core_DomainStateQuerySelector": { "level": "DEBUG" }
  }
}
