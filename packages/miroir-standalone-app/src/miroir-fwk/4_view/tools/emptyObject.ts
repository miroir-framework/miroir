// No imports on purpose: shared by route pages and by the component test tools (#286),
// so that importing this constant does not pull a route page into a bundle chunk.
export const emptyObject = {};
