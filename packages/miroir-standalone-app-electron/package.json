{
  "name": "miroir-standalone-app-electron",
  "version": "1.0.0",
  "description": "Electron desktop application wrapper for miroir-standalone-app",
  "author": "Miroir Framework <miroir-framework@protonmail.com>",
  "homepage": "https://github.com/miroir-framework/miroir/packages/miroir-standalone-app-electron/#readme",
  "bugs": "https://github.com/miroir-framework/miroir/issues",
  "license": "MIT",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "build": "npx tsc --noEmit --skipLibCheck",
    "build-main": "tsc src/main.ts --outDir dist --target es2020 --module commonjs --moduleResolution node --esModuleInterop --allowSyntheticDefaultImports --strict --skipLibCheck",
    "build-preload": "tsc src/preload.ts --outDir dist --target es2020 --module commonjs --moduleResolution node --esModuleInterop --allowSyntheticDefaultImports --strict --skipLibCheck",
    "build-electron": "npm run build-main && npm run build-preload",
    "electron": "npm run build-electron && electron .",
    "electron-dev": "npm run build-electron && electron . --dev",
    "pack": "npm run build-electron && electron-builder --dir",
    "dist": "npm run build-electron && electron-builder",
    "dist-win": "npm run build-electron && electron-builder --win",
    "dist-mac": "npm run build-electron && electron-builder --mac",
    "dist-linux": "npm run build-electron && electron-builder --linux"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.1",
    "miroir-standalone-app": "*"
  },
  "devDependencies": {
    "@types/node": "^20.8.9",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "typescript": "^4.9.4"
  },
  "build": {
    "appId": "com.miroir-framework.miroir-standalone-app-electron",
    "productName": "Miroir Standalone App",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../miroir-standalone-app/dist",
        "to": "app",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/icon.ico"
    },
    "mac": {
      "target": "dmg",
      "icon": "assets/icon.icns"
    },
    "linux": {
      "target": "AppImage",
      "icon": "assets/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
