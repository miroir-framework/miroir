export default {}


/**
 * the remote data store should have 2 implementations (?)
 * * the client (browser) implementation shall send REST/GraphQL queries
 * * the server (nodejs) implementation shall send SQL/NoSQL queries, depending on used backend.
 */
export class RemoteDataStoreController {
  
}